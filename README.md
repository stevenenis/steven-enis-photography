# Steven Enis — Photography

Portfolio site for Steven Enis (@stevenaubrey_). Static site — no build step.

## Structure
- `index.html` — the page
- `styles.css` — all styling
- `script.js` — nav, scroll reveals, and the photo lightbox
- `images/` — photographs + the About portrait

## Deploy (Cloudflare Pages)
Connected to this repo. No framework, no build command.
- Build command: *(leave blank)*
- Build output directory: `/`

Any push to the `main` branch redeploys the live site.
